# TODO: Vectors as module attributes need to be returned by memory
#       address instead of being copied.

# TODO: Consider a user-defined "TYPE" that only contains one of the
#       fundamental types to simply be of that type (no struct).
# TODO: User defined "TYPE" can only contain fundamental types
#       (logical, character, integer, real) and other valid user
#       defined types.
# TODO: The interface being used by a subroutine-as-an-argument could
#       be defined in another module. Need to check for that.
#       Need to disallow "EXTERNAL" subroutines-as-arguments.


# TODO: "LOGICAL A" is a valid declaration that is not accepted, need
#       to look into when the "::" is required.
